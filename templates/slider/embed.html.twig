$(document).ready(function(){
    if($sliderId = $('#shopmacher-slider').attr('slider-id')) {
        $('<script src="{{ app_url }}/slider/lib.js?slider-id='+$sliderId+'"></script>').appendTo($('body'));
    }
});
